[Unit]
Description=arbbot naked-leg hedger (only when completing the basket locks profit)
[Service]
Type=oneshot
WorkingDirectory=%h/claude/arbbot
Environment=ARBBOT_CREDENTIALS_DIR=%h/.arbbot-credentials
ExecStart=%h/claude/arbbot/.venv313/bin/python scripts/hedge_naked_legs.py --live
