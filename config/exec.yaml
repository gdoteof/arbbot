# Trading capital comes from authenticated venue snapshots, refreshed automatically.
# Original deposits live in config/funding.yaml and are reporting-only.
capital_source: venue
# Legacy concentration setting. Production class/global ceilings currently share
# the explicit 85% deployment policy shown on the Capital page; neither uses deposits.
per_class_cap: 0.50
